# Pi 0.87.1 subscription shortlist

Date: 2026-09-23. Mode: refresh. Interaction policy: confirm uncertainty.
Scope: Pi CLI catalog, fixed thinking execution, Playground and Desktop consumers.

## Evidence and operator decisions

The corrected operator paste identifies Pi v0.87.1 and these six rows, in order:

- gpt-5.6-luna [openai-codex]
- gpt-5.6-sol [openai-codex]
- gpt-5.6-terra [openai-codex]
- gpt-6-astra [openai-codex]
- gpt-6-luna [openai-codex]
- gpt-6-sol [openai-codex]

All six use thinking `medium`. The operator explicitly requires the visible
`[openai-codex]` suffix to identify subscription routing, and separately authorized
six fixed combinations plus the missing execution support. The earlier
session-shaped version value was superseded by the corrected paste; it is not CLI
version evidence. Installed package metadata independently reports 0.87.1.

This is the complete Cats shortlist for the selected subscription provider, not
all upstream Pi providers or effort menus. No model/effort default was supplied;
the first row initializes the UI without a default marker. Display names retain
exact lowercase spelling and the bracketed provider, followed by ` — medium` to
identify the fixed combination. Custom input keeps the existing `provider/model`
contract. No entitlement claim is made for other accounts.

[Operator evidence](./fixtures/pi-0.87.1/operator.redacted.txt) and
[execution mapping projection](./fixtures/pi-0.87.1/execution-mapping.redacted.json).
The intake tree retains provider -> model -> thinking -> medium; assessment marks
the authorized changes ready without projection loss or outstanding questions.

## Execution mapping

The installed `@earendil-works/pi-coding-agent` package supplies bounded, unbundled
transport evidence in `dist/cli/args.js` and `dist/main.js`. The former accepts
`--provider`, `--model` and `--thinking medium`; the latter gives explicitly parsed
thinking precedence over saved/scoped defaults. Source file hashes are retained
in the projection, which is not a verbatim CLI capture or live model enumeration.

Cats stores `openai-codex/<raw model ID>` and passes separate provider/model flags
plus `--thinking medium` for the approved combinations. Both structured selection
and plain model strings apply the fixed value; new and resumed RPC launches use
the same path. Bracketed display text is never sent as part of the raw model ID.
Other custom provider/model strings retain their prior transport without acquiring
thinking metadata. No native CLI session, login, model listing or inference ran.

## Catalog and consumer behavior

- Curated YAML preserves the provider hierarchy and fixed thinking option.
- Immediate and refreshed shortlist reads retain six ordered rows, with no dynamic
  enumeration. Static fallback and fixed execution knowledge match the shortlist.
- Playground supports six-plus-custom choices, serialization and saved custom values.
- Desktop fallback and mounted selector retain the subscription suffix, first
  emitted selection, switching and reopening without editable effort/default labels.
- Dynamic Pi discovery remains tested separately with empty curated fixtures
  installed before service/server construction, including live diagnostics.
  These fixtures retain historical discovery IDs without inheriting the removed
  static default. A curated row default does not configure dynamic discovery.
- The generic unverified-metadata test uses an explicitly unverified provider now
  that Pi has a verified fixed-combination manifest.
- Exact old-ID searches classified discovery/session-history fixtures and the
  OpenClaw gateway example as independent data; they were retained. The bundled
  Pi fallback/immediate consumers were updated.

## Validation and personal configuration

- Passed: Runtime UI generation and TypeScript checking; 136 focused catalog,
  advanced selection, Pi parser/adapter/discovery and Playground tests; two Pi
  HTTP tests and one live-diagnostics test using mocked CLI execution.
- Initial discovery/diagnostics failures exposed obsolete default expectations.
  Tests now isolate unrestricted discovery with an empty curated fixture, assert
  no inferred default, and respect diagnostics' omitted-default detail field.
  No dynamic discovery production behavior was changed to satisfy these tests.
- Desktop validation passed 59 focused tests after rebuilding the host artifacts;
  server/test-UI/host builds and test TypeScript checking passed. Initial host
  test failures came from stale generated artifacts and required no source fix.
- Canonical skill feedback records label/provider/argument separation, bounded
  installed-package evidence, fixed thinking execution, and independent dynamic
  discovery fixtures. Both member and parent Codex/Claude mirrors match all 25
  canonical files; frontmatter and relative links passed validation.
- The operator authorized the personal Pi block addition. Before writing, the
  current file matched the reviewed snapshot and all other provider sections
  matched. A backup was written as
  `curated-model-catalogs.yaml.bak-pi-2026-09-22T22-28-46-427Z`; readback confirmed
  Pi 0.87.1 with six entries and unchanged other providers.

No native Pi inference session or installed Desktop binary was exercised. The
focused checks do not represent a full-suite run. No commit, PR, release or
version bump is included in the catalog-refresh request.
