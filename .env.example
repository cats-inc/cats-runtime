# cats-runtime local configuration
# Copy to `.env` for local development. Do not commit `.env`.

# Listener
CATS_RUNTIME_HOST=127.0.0.1
CATS_RUNTIME_PORT=3110

# Optional inbound auth for clients calling cats-runtime.
CATS_RUNTIME_API_KEY=

# Upstream backend
AGENT_FLEET_BASE_URL=http://localhost:3100
AGENT_FLEET_API_KEY=

# Shared timeout for upstream requests, including streamed turns.
AGENT_FLEET_TIMEOUT_MS=600000
